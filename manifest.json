{
  "$comment": "Story manifest — engine v0.28+ fetches this at boot to discover extra stories. Each entry is a same-origin URL relative to game.html. Drop a story.json into this folder, add its path here, redeploy the manifest. No engine edit required.",
  "stories": [
    "./hollow-forest.json"
  ]
}
